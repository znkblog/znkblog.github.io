---
import '@picocss/pico/css/pico.min.css';

const site = {
  title: "znk 블로그",
};
---

<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{site.title}</title>
    <link href="https://cdn.jsdelivr.net/gh/wan2land/d2coding/d2coding-full.css" rel="stylesheet" />
  </head>
  <body>
    <div class="layout">
      <div class="container">
        <header>
          <nav>
            <ul>
              <li>👍 {site.title.toUpperCase()}</li>
            </ul>
            <ul>
              <li><a href="/">HOME</a></li>
              <li><a href="/blog">BLOG</a></li>
            </ul>
          </nav>
        </header>
      </div>
      <div class="container">
        <slot />
      </div>
      <div class="container last">
        <footer>
          <div>This blog is degined by ZNK,</div>
          <div>built with ASTRO,</div>
          <div>and powered by GITHUB PAGES.</div>
        </footer>
      </div>
    </div>
  </body>
</html>

<style lang="scss">
  // :root {
  //   --pico-font-family: 'D2Coding', monospace;
  // }
  // * {
  //   font-family: 'D2Coding', monospace !important;
  // }
  :root {
    --pico-font-family: 'D2Coding', ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace, var(--pico-font-family-emoji);
    --pico-font-family-monospace: D2Coding, ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace, var(--pico-font-family-emoji);
  }
  body {
    
    position: fixed; top: 0; right: 0; bottom: 0; left: 0;
  }
  .layout {
    overflow-y: scroll;
    height: 100%;
  }
  header {
    margin: 1rem 0;
  }
  .last {
    position: sticky; top: 100vh;
  }
  footer {
    margin: 3rem 0;
    padding-bottom: 3rem;

    display: flex; justify-content: center; align-items: center;
    flex-wrap: wrap; column-gap: 1rem;

  }
</style>